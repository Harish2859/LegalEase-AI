# TODO: RAGAS evaluation against CUAD dataset
# pip install ragas datasets

from datasets import load_dataset

def run_eval():
    # dataset = load_dataset("cuad")
    # TODO: run RAG pipeline over samples and score with RAGAS
    pass

if __name__ == "__main__":
    run_eval()
